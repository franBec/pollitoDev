{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start Hugo Server",
            "type": "node-terminal",
            "command": "npm start",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}
